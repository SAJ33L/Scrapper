requests==2.31.0
beautifulsoup4==4.12.3
lxml==5.2.1
brotlicffi>=1.0.0
playwright==1.44.0
pandas==2.2.2
openpyxl==3.1.5

# Playwright runtime setup (system-level, not pip-installable):
# 1) playwright install chromium
# 2) sudo playwright install-deps
